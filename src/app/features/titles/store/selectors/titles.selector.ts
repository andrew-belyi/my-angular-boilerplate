import { createSelector } from '@ngrx/store';

import { selectTitlesState } from '@features/titles/store/selectors/index';

// export const selectTitles = createSelector(selectTitlesState, someFnHere);
